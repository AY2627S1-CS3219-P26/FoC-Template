# credit-service

See ../AGENTS.md for repository-wide rules.

This service owns the domain rules for the closed credit economy. The web client presents them and
never reimplements them, so any rule that belongs here stays here.

Nothing is implemented yet and no language, framework or database has been
decided. Do not assume one.
